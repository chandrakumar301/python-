#program to read a string and return second word in upper 
def fun(str,b):
    k=0
    for i in range(0,b):
            k=str[3].upper()
            return k
str="chandra"     
b=len(str)
print(fun(str,b))
